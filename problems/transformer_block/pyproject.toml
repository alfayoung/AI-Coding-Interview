[project]
name = "transformer-block"
version = "0.1.0"
description = "Implement a Transformer encoder block from scratch"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.10.0",
    "numpy",
]
